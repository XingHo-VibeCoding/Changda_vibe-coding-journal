# 个人主页 PRD（PRD.md）

> Day 4 产出 · 2026-09-20 · 项目名：**个人主页**
> 上游：`research.md`（Day 3）。本文档把研究结论翻译成"做到什么样算做完"。

## 一、项目概述

- **项目名**：个人主页（vibe coding 学习项目实战产物）
- **面向用户**：① 想了解"我是谁 / 我能做什么"的朋友与潜在合作方 ② 自己（作为作品集和成长记录）
- **核心价值**：互联网上的"自己的地"——自我介绍、作品展示、联系方式的单一入口，完全可控，改起来不求人。
- **首版范围**：2 个静态页面，纯前端、无后端、无登录、无数据库。

## 二、用户故事

1. **作为**陌生访客，**我想**在一个页面看到你的名字、一句话介绍和联系方式，**以便**快速决定要不要进一步联系。
2. **作为**潜在合作方，**我想**点进你的作品卡片看到项目详情，**以便**判断你的能力是否符合需要。
3. **作为**你自己，**我想**改完一段内容后能立刻在本地预览，**以便**不依赖网络也能迭代。

## 三、页面与功能概览

本 MVP 由 **2 个静态页面** 组成，无后端、无数据库、无登录系统。

| 页面 | 文件 | 核心职责 |
|---|---|---|
| 首页 | `index.html` | 一屏装下"个人主页核心四件套"：名字 / 一句话 / 联系方式 / 作品卡片 |
| 作品详情页 | `work.html` | 从首页点进来，看单个作品的简介、过程、收获 |

> 「联系方式」的具体形式（邮箱 / 微信 / 社交链接）属于**设计选型**，见 Day 5，PRD 不在此强约束。

## 四、功能清单 + 验收标准 ⭐

> 验收标准统一格式：「在什么条件下，能看到/做到 X」。每条 pass/fail 一眼判定。

### 功能 F1：首页 `index.html`

| # | 验收标准 | 类型 |
|---|---|---|
| F1-1 | 在桌面浏览器（宽度 ≥ 1024px）打开 `index.html`，能看到：名字、一句话介绍、联系方式区块 | 可勾选 |
| F1-2 | 在移动端浏览器（宽度 ≤ 360px）打开同一文件，**无横向滚动条**，所有文字不被截断 | 可勾选 |
| F1-3 | 浏览器标签页（`<title>`）包含「个人主页」或你的名字 | 可勾选 |
| F1-4 | 首页上至少有 **2 个**可点击的作品卡片（每个卡片显示标题 + 一句话说明） | 可勾选 |
| F1-5 | 点击任一作品卡片，**会跳转到 `work.html?title=<作品名>`**（hash 或 query 参数均可） | 可勾选 |
| F1-6 | 在浏览器刷新首页（F5），**不报错**，不出现 404 | 可勾选 |
| F1-7 | 首页源码里**不包含**任何 `<script>` 外链到第三方域名；CSS 只用内联或同目录文件 | 可勾选 |
| F1-8 | 用浏览器开发者工具的 Console 面板查看，**无报错、无警告** | 可勾选 |

### 功能 F2：作品详情页 `work.html`

| # | 验收标准 | 类型 |
|---|---|---|
| F2-1 | 直接访问 `work.html`（不带参数），页面**仍能正常打开**，且显示明确的「请从首页进入」提示文本与返回首页的链接 | 可勾选 |
| F2-2 | 从首页点击卡片跳转到 `work.html?title=<作品名>` 后，页面**正确显示该作品名**作为页面主标题 | 可勾选 |
| F2-3 | 详情页上有一个**"← 返回首页"链接**，点击后能跳回 `index.html` | 可勾选 |
| F2-4 | 详情页内容区域至少包含三段文字："作品简介 / 我做了什么 / 学到什么" | 可勾选 |
| F2-5 | 详情页在 ≤ 360px 宽度下**不溢出**，文字不被截断 | 可勾选 |
| F2-6 | 详情页 Console **无报错、无警告** | 可勾选 |

### 功能 F3：本地预览与改动流程（人为流程验收，不是页面功能）

| # | 验收标准 | 类型 |
|---|---|---|
| F3-1 | 修改首页或详情页内容后，用浏览器打开本地 HTML 文件，**改动立刻可见**（无需构建、无需服务器） | 可勾选 |
| F3-2 | 项目根目录下运行 `git status`，只显示**改动了的 HTML 文件**，不出现 `.workbuddy/`、`.env`、`desktop.ini` 等敏感文件 | 可勾选 |

## 五、不在范围内（本期不做）❗

以下功能**第一版不实现**——任何时候发现有人想加这些，回答统一是"不在本期"：

- 用户登录 / 注册系统
- 评论 / 留言 / 点赞
- 博客 / 文章列表 / 文章详情
- 后端 API / 数据库 / 服务端渲染
- 多语言切换
- 深色模式 / 主题切换
- SEO 优化 / 访问统计 / 数据埋点

> 理由完整版见 `research.md` 第四节。本节是它的"落地版"。

---

## 六、对开发阶段（Day 7 起）的提示

- **技术选型**（HTML / CSS 用纯手写还是用框架）见 Day 5，本 PRD 不绑定技术
- **设计风格**（配色 / 字体 / 版式）见 Day 5–6，本 PRD 不绑定长相
- 本 PRD 只回答"做到什么样算做完"，**不回答"怎么做"和"长什么样"**